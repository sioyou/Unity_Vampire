using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Managers : MonoBehaviour
{
    public static Vector2 moveDir;          // static은 객체에 존속성을 가지지 않게해줌 -> 다른 메모리 어딘가에 따로 할당해서 어디서든 사용가능

    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
